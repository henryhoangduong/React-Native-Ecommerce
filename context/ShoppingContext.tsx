import { createContext } from "react";
import { useContext } from "react";

type SHOPPINGTYPE = {
  fetchItems: () => void;
};

const ShoppingContextProvider = () => {
  const fetchItems = () => {};
  return <></>;
};
